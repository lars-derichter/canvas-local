{
  "name": "cw-smoke-harness",
  "displayName": "Coursewright smoke-test harness",
  "description": "Empty extension whose only job is to put the test window into extension development mode, so --extensionTestsPath runs. The extension under test is installed from its .vsix instead, and this contributes nothing.",
  "publisher": "local",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "vscode": "^1.93.0"
  },
  "contributes": {}
}
